export type GenderType = 'Male' | 'Female' | 'Other';

export interface Student {
  name: string;
  class: string;
  section: string;
  gender: string;
  parentName: string;
  email: string;
  phone: string;
  dateOfBirth: string;
  admissionDate: string;
  address: string;
}
